using UnityEngine;

public class GameStatus : MonoBehaviour
{
    // might not need this. publically accessible script that shows the game's start status and other events.
    // Similar methods are available in GameplayManager!
}
