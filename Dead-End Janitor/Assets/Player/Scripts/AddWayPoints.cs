using UnityEngine;

//This script is meant to be used to add waypoints more easily. Attach to the player. Press 1 to add a new path. Press 2 to add a new waypoint to a path.
//TODO!

public class AddWayPoints : MonoBehaviour
{
/*  public GameObject waypoint;
  public Waypoint current;
  public GameObject path;
  public GameObject currentPath;
  // Update is called once per frame
  void Update()
  {
    if(Input.GetKeyDown('1')){
      Waypoint newPoint = Instantiate(path, transform.position, transfrom.rotation, currentPath).GetComponent<Waypoint>();
      current.SetNext(newPoint);
      current = newPoint;
    }
//    else if(Input.GetKeyDown('2'))
  }*/
}
